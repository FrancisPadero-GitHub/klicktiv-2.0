# Thes are only safe if you have enabled RLS in your Supabase project. 
# If you have not, then these keys can be used to access your database and should be kept secret.
# But for most part of the cases, these keys are safe to be public as they only have access to the tables and rows that you have defined in your RLS policies.
NEXT_PUBLIC_SUPABASE_URL = "https://cetolgksppouyjeluvfe.supabase.co"
# This is called "Publishable key" now in supabase, but it is the same as the "anon key" that we used to use.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY = "sb_publishable_COE0CBpz-S6efmm_yO-XSQ_fnF0qWqZ"

# Command to re fetch data types in supabase
# npx supabase gen types typescript --project-id cetolgksppouyjeluvfe > database.types.ts